{{> _head}}

{{> _site-header}}

<main>
  <section>


  <ol class="evenGrid">
    {{#each work-items}}
      <li class="">
      <a href="{{ url }}" title="Read more about {{ title }}">
        <div class="evenGrid-itemWrapper">
          <div class="evenGrid-item">

            <h2>{{ title }}</h2>
            <p>{{ headline }}</p>

          </div>
        </div>
      </a>
      </li>
    {{/each}}
  </ol>


  </section>
</main>

{{> _footer}}
