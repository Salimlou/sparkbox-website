{{> _head}}

{{> _site-header}}

<main>
  <section>


  <ol class="evenGrid">
    {{#each work-items}}
      <li class="evenGrid-item">
      <a href="{{ url }}">
        <div class="thing">
          <h2>{{ title }}</h2>
          <p>{{ headline }}</p>
        </div>
      </a>
      </li>
    {{/each}}
  </ol>


  </section>
</main>

{{> _footer}}
